# Core has no runtime dependencies. Everything below is optional and
# enables a specific extractor or roadmap feature.

# testing
pytest>=7.4

# v0.2 -- PE parsing. Optional, and staying that way: unlike numpy, its
# absence removes findings rather than speed, so it must be reported and never
# degrade silently. Without it the tests that verify the synthetic PE fixture
# against a real parser skip and the rest of the suite runs unchanged.
# pefile>=2023.2.7

# v0.3 -- YARA matching
# yara-python>=4.3

# fuzzy hashing (needs libfuzzy installed)
# ssdeep>=3.4

# optional accelerator for byte counting. Roughly 3x faster entropy on large
# samples. The standard-library fallback produces identical numbers.
# numpy>=1.24

# v0.6 -- classifier
# scikit-learn>=1.3
# lightgbm>=4.0
# numpy>=1.24
